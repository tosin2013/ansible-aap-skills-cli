# Local preview override — use this with:
#   bundle exec jekyll serve --config _config.yml,_config.local.yml
#
# Overrides remote_theme with the locally-installed gem so layouts resolve
# without a network call to GitHub. Also clears baseurl so links work at
# http://localhost:4000.

baseurl: ""
theme: just-the-docs
remote_theme: ""
